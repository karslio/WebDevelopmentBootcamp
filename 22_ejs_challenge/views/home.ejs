<%- include('partials/header'); -%>
<h1>HOME</h1>

<p><%= startingContent %></p>
<div>
    <% posts.forEach(post => {  %>
    <h1><%= post.title %></h1>
    <p><%= post.body %></p>
    <% }); %>
</div>

<%- include('partials/footer'); -%>